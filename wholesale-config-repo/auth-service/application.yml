server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: rootpassword

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh